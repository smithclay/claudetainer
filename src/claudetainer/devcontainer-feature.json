{
  "name": "claudetainer",
  "id": "claudetainer",
  "version": "0.1.0",
  "description": "Language support for Claude Code through automated hooks and commands",
  "options": {
    "include": {
      "type": "string",
      "default": "",
      "description": "Comma-separated list of presets (empty for Phase 1)"
    },
    "includeBase": {
      "type": "boolean",
      "default": true,
      "description": "Include universal commands"
    }
  }
}